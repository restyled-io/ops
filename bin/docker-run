#!/bin/sh
set -e

docker_run() {
  if [ -t 1 ]; then
    exec docker run --tty "$@"
  else
    exec docker run "$@"
  fi
}

if [ "${BUILD:-1}" -eq 1 ]; then
  make build >&2
fi

docker_run --rm --interactive --env-file "$PWD"/.env restyled/ops "$@"
